# nextest profile for the ncz-os quality gate. Small retry budget absorbs
# load-sensitive flakes (e.g. TUI/probe timing tests) without masking real
# failures — a genuine failure fails all attempts.
[profile.default]
retries = 2
fail-fast = false
